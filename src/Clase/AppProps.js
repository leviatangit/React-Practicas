import {React , Component } from 'react';

/**
 * Props
 * 
 * 
 * 
 */

class AppProps extends Component {
  render () {
    <div class="list">
      <input name="item" className="list" value="{ this.props.value }" /> 
    </div>
  }
}

export default AppProps;
